# 3b단계: 태그 페이지 · i18n 라우팅 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 태그를 눌러 같은 태그의 글을 모아 보고, 영어 글이 `/en/posts`에 별도 라우트로 뜨며, 번역이 있는 글에서 언어를 전환할 수 있다.

**Architecture:** 태그 집계와 언어 판단은 전부 `src/lib`의 순수 함수로 두고 Vitest로 검증한다. 글 상세·아카이브의 본문은 컴포넌트로 추출해 한국어·영어 라우트가 공유하고, 라우트 파일에는 언어별로 달라지는 `getStaticPaths`만 남긴다. "UI 문구 번역은 하지 않는다" — 영어 페이지도 한국어 크롬(헤더·푸터·제목 문구)을 그대로 쓰고 글 본문만 영어다.

**Tech Stack:** Astro 7, React 19, Tailwind CSS 4, MDX, Vitest

## Global Constraints

- 색·간격 값은 `src/styles/global.css`의 `@theme` 블록에만 정의한다. 컴포넌트에 hex/px/oklch 색상값을 직접 박지 않는다.
- "이 글이 무슨 언어인가"를 판단하는 곳은 `src/lib/posts.ts`의 `parseEntryId` 하나뿐이어야 한다. 라우트·목록·언어 전환이 모두 이 함수를 거친다.
- **UI 문구는 번역하지 않는다.** 헤더 nav, "기록의 목차", "분 읽기" 같은 크롬은 한국어로 둔다. 새로 만드는 UI는 언어 전환 컨트롤 하나뿐이다.
- 어떤 링크도 404를 내면 안 된다. 목록·전환 UI가 가리키는 곳은 반드시 생성되는 페이지여야 한다.
- nav `active`는 `NavKey` 유니온이다(3a). 새 값이 필요하면 `src/lib/nav.ts`의 유니온에 먼저 추가한다.
- 다크모드는 구현하지 않는다. TypeScript는 **6.x 고정**.
- 각 태스크 끝에서 `pnpm test`·`pnpm check`·`pnpm build`가 통과해야 한다.

## 이 계획이 다루지 않는 것

- **Pagefind 검색은 3c다.** 별도 툴체인(postbuild 인덱싱, 빌드 후에만 동작)이라 분리했다. 헤더의 `글 검색` 알약은 3c까지 `/posts`를 가리킨 채 둔다.
- 영어 홈·기술·소개 페이지는 만들지 않는다. 영어는 글(아카이브+상세)만 있다.
- UI 문구 번역, giscus 댓글(저장소 생성 후 4단계).

---

### Task 1: 태그 집계 순수 함수

**Files:**
- Modify: `src/lib/posts.ts`
- Modify: `src/lib/posts.test.ts`

**Interfaces:**
- Consumes: `PostSummary`, `sortByDateDesc` from `src/lib/posts.ts`
- Produces:
  - `interface TagCount { tag: string; count: number }`
  - `collectTags(posts: PostSummary[]): TagCount[]` — 빈도 내림차순, 동률이면 가나다/사전순
  - `postsWithTag(posts: PostSummary[], tag: string): PostSummary[]` — 최신순, 해당 태그를 가진 글만

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/posts.test.ts`의 마지막 `describe` 뒤에 추가한다. 파일 상단 import에 `collectTags`, `postsWithTag`를 더한다.

```ts
describe('collectTags', () => {
  it('태그를 빈도 내림차순으로 세고, 동률이면 사전순으로 낸다', () => {
    const result = collectTags([
      post({ slug: 'a', pubDate: '2024-01-01', tags: ['TCP', '네트워크'] }),
      post({ slug: 'b', pubDate: '2023-01-01', tags: ['TCP'] }),
      post({ slug: 'c', pubDate: '2022-01-01', tags: ['DB'] }),
    ]);
    expect(result).toEqual([
      { tag: 'TCP', count: 2 },
      { tag: 'DB', count: 1 },
      { tag: '네트워크', count: 1 },
    ]);
  });

  it('빈 목록은 빈 배열이 된다', () => {
    expect(collectTags([])).toEqual([]);
  });
});

describe('postsWithTag', () => {
  it('해당 태그를 가진 글만 최신순으로 낸다', () => {
    const result = postsWithTag(
      [
        post({ slug: 'old', pubDate: '2022-01-01', tags: ['TCP'] }),
        post({ slug: 'new', pubDate: '2024-01-01', tags: ['TCP'] }),
        post({ slug: 'other', pubDate: '2023-01-01', tags: ['DB'] }),
      ],
      'TCP',
    );
    expect(result.map((p) => p.slug)).toEqual(['new', 'old']);
  });

  it('태그가 없으면 빈 배열이 된다', () => {
    expect(postsWithTag([post({ slug: 'a', pubDate: '2024-01-01', tags: ['TCP'] })], 'DB')).toEqual(
      [],
    );
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `pnpm test src/lib/posts.test.ts`
Expected: FAIL — `collectTags is not a function` 류

- [ ] **Step 3: 구현**

`src/lib/posts.ts` 맨 끝에 추가한다.

```ts
export interface TagCount {
  tag: string;
  count: number;
}

/** 태그를 빈도 내림차순으로 집계한다. 동률이면 사전순으로 안정 정렬한다. */
export function collectTags(posts: PostSummary[]): TagCount[] {
  const counts = new Map<string, number>();
  for (const p of posts) {
    for (const tag of p.tags) counts.set(tag, (counts.get(tag) ?? 0) + 1);
  }
  return [...counts.entries()]
    .map(([tag, count]) => ({ tag, count }))
    .sort((a, b) => b.count - a.count || a.tag.localeCompare(b.tag));
}

export function postsWithTag(posts: PostSummary[], tag: string): PostSummary[] {
  return sortByDateDesc(posts.filter((p) => p.tags.includes(tag)));
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm test src/lib/posts.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/lib/posts.ts src/lib/posts.test.ts
git commit -m "태그 집계 순수 함수 추가 (collectTags·postsWithTag)"
```

---

### Task 2: 태그 목록 · 태그별 글 페이지

**Files:**
- Create: `src/pages/tags/index.astro`
- Create: `src/pages/tags/[tag].astro`
- Modify: `src/components/PostMeta.astro` (태그를 링크로)

**Interfaces:**
- Consumes: `loadPosts` from `src/lib/collection`; `collectTags`, `postsWithTag` from `src/lib/posts`
- Produces: 라우트 `/tags`, `/tags/<tag>`

태그는 한국어 글 기준으로 만든다(영어 태그 페이지는 범위 밖). 태그 문자열에는 한글·공백이 들어가므로 href는 `encodeURIComponent`로 감싼다. Astro가 `getStaticPaths`의 `params`를 자동 인코딩하므로, 파라미터에는 **원본 태그 문자열**을 그대로 넣는다.

- [ ] **Step 1: 태그 목록 페이지**

`src/pages/tags/index.astro`:

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { loadPosts } from '../../lib/collection';
import { collectTags } from '../../lib/posts';

const tags = collectTags(await loadPosts('ko'));
---

<BaseLayout title="태그 · 씨유" description="태그로 글을 모아 봅니다." narrow>
  <section class="pt-16 pb-10">
    <div class="mb-4.5 text-xs font-semibold tracking-[0.16em] text-ink-faint uppercase">
      태그
    </div>
    <h1 class="m-0 text-[30px] leading-[1.15] font-extrabold tracking-[-0.03em] sm:text-[44px] sm:leading-[1.12]">
      태그로 찾기
    </h1>
  </section>

  <div class="flex flex-wrap gap-2.5 pb-18">
    {
      tags.map((t) => (
        <a
          href={`/tags/${encodeURIComponent(t.tag)}`}
          class="flex items-center gap-2 rounded-pill border border-ink/16 px-4 py-2 text-[13px] font-medium text-ink-subtle transition-colors hover:border-ink/32 hover:text-ink"
        >
          <span>#{t.tag}</span>
          <span class="font-mono text-[11px] text-ink-faint">{t.count}</span>
        </a>
      ))
    }
  </div>
</BaseLayout>
```

- [ ] **Step 2: 태그별 글 페이지**

`src/pages/tags/[tag].astro`:

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { loadPosts } from '../../lib/collection';
import { collectTags, postsWithTag } from '../../lib/posts';
import { categoryLabel } from '../../lib/categories';
import { formatShortDate } from '../../lib/date';

export async function getStaticPaths() {
  const posts = await loadPosts('ko');
  return collectTags(posts).map((t) => ({
    params: { tag: t.tag },
    props: { posts: postsWithTag(posts, t.tag) },
  }));
}

const { tag } = Astro.params;
const { posts } = Astro.props;
---

<BaseLayout title={`#${tag} · 씨유`} description={`${tag} 태그가 붙은 글.`} narrow>
  <section class="pt-16 pb-8">
    <a href="/tags" class="text-[13px] text-ink-faint transition-colors hover:text-ink-subtle">
      ← 태그 목록
    </a>
    <h1 class="mt-6 mb-0 text-[30px] leading-[1.15] font-extrabold tracking-[-0.03em] sm:text-[44px] sm:leading-[1.12]">
      #{tag}
    </h1>
    <p class="mt-3 text-[15px] text-ink-muted">{posts.length}편</p>
  </section>

  <div class="pb-18">
    {
      posts.map((p) => (
        <a
          href={p.href}
          class="flex flex-col gap-1 border-t border-ink/8 py-4 transition-opacity hover:opacity-60 sm:grid sm:grid-cols-[1fr_auto] sm:items-baseline sm:gap-5"
        >
          <div class="flex flex-wrap items-baseline gap-3">
            <span class="text-[10.5px] font-semibold tracking-[0.06em] text-accent uppercase sm:min-w-16">
              {categoryLabel(p.category)}
            </span>
            <span class="text-[17px] font-semibold tracking-[-0.01em]">{p.title}</span>
          </div>
          <span class="font-mono text-[12.5px] whitespace-nowrap text-ink-faint">
            {formatShortDate(p.pubDate)} · {p.minutes}분
          </span>
        </a>
      ))
    }
  </div>
</BaseLayout>
```

- [ ] **Step 3: 글 상세의 태그를 링크로 바꾼다**

`src/components/PostMeta.astro`에서 태그 span을 a로 바꾼다. 기존:

```astro
      {post.tags.map((tag) => (
        <span class="rounded-[14px] bg-ink/5 px-3 py-[5px] text-xs text-ink-subtle">#{tag}</span>
      ))}
```

교체:

```astro
      {post.tags.map((tag) => (
        <a
          href={`/tags/${encodeURIComponent(tag)}`}
          class="rounded-[14px] bg-ink/5 px-3 py-[5px] text-xs text-ink-subtle transition-colors hover:bg-ink/10 hover:text-ink"
        >
          #{tag}
        </a>
      ))}
```

- [ ] **Step 4: 빌드하고 라우트·인코딩 확인**

Run: `pnpm build 2>&1 | grep -E "tags/|page"`
Expected: `/tags/index.html`과 태그별 페이지들이 생성된다. 한글 태그는 퍼센트 인코딩된 디렉터리로 나온다(예: `tags/%EB%84%A4%ED%8A%B8%EC%9B%8C%ED%81%AC/`).

Run: `ls dist/tags/`
Expected: `index.html`과 각 태그 디렉터리. 태그 수는 `collectTags` 결과와 같아야 한다(현재 시드 기준 TCP·네트워크·트러블슈팅·로드밸런싱·회고).

- [ ] **Step 5: 브라우저로 왕복 확인**

`pnpm dev` 후 `/tags`에서 `#TCP`를 클릭 → `/tags/TCP`로 이동하고 TCP 태그 글 2편(TCP 에러 복구, 그리고 태그가 겹치는 글)이 나오는지 확인. 글 상세(`/posts/tcp-error-recovery`)의 태그 `#네트워크`를 클릭 → `/tags/네트워크`로 이동하는지 확인.

- [ ] **Step 6: 타입체크와 커밋**

Run: `pnpm check`
Expected: `0 errors`

```bash
git add -A
git commit -m "태그 목록과 태그별 글 페이지 추가

글 상세의 태그를 /tags/<tag> 링크로 연결한다. 한글 태그는 params에
원본을 넣고 href만 encodeURIComponent로 감싼다(Astro가 params를 자동 인코딩)."
```

---

### Task 3: 영어 시드 글

**Files:**
- Create: `src/content/posts/en/tcp-error-recovery.mdx`

**Interfaces:**
- Consumes: 없음 (콘텐츠)
- Produces: `en/tcp-error-recovery` 엔트리 — 이후 태스크의 `/en/` 라우트가 이걸 렌더한다

`ko/tcp-error-recovery.mdx`의 영어 번역본이다. slug가 같아야(`tcp-error-recovery`) 언어 전환이 번역 쌍으로 이어진다.

- [ ] **Step 1: 번역본 작성**

`src/content/posts/en/tcp-error-recovery.mdx`:

````mdx
---
title: A Look at TCP Error Recovery
description: Following how retransmission actually works through real scenarios.
pubDate: 2023-11-09
category: infra
tags: ['TCP', 'network', 'troubleshooting']
---

Even a connection that looks stable constantly meets packet loss, delay, and reordering. The reliable stream we get exists thanks to the intricate recovery mechanisms running underneath.

## Two triggers for retransmission

TCP detects loss two ways: a timeout (RTO), and fast retransmit via duplicate ACKs. The former is conservative and slow; the latter is aggressive and fast.

> On three duplicate ACKs, TCP retransmits the segment immediately without waiting for a timeout.

## How RTO is computed

RTO is not a fixed value — it is continuously estimated from the observed round-trip time (RTT).

```js
// Jacobson/Karels RTO estimation
SRTT   = (1 - a) * SRTT   + a * R        // a = 1/8
RTTVAR = (1 - b) * RTTVAR + b * abs(SRTT - R)
RTO    = SRTT + 4 * RTTVAR               // clamped to a 1s minimum
```

If no ACK arrives within the estimated `RTO`, the segment is treated as lost and RTO doubles exponentially.

## Confirming in practice

To observe retransmission in real traffic, capture with `tcpdump` and follow the flow.

```bash
# filter only retransmissions to a specific host
tcpdump -i eth0 'tcp[tcpflags] & tcp-push != 0' \
  -n host 10.0.1.42
```

Once the retransmission rate climbs past 1%, it usually signals a bottleneck in the network path or the receive buffer.

## Wrap-up

TCP's reliability is not magic — it is the repetition of observe, estimate, retransmit.
````

- [ ] **Step 2: 컬렉션이 영어 글을 잡는지 확인**

Run: `pnpm build 2>&1 | grep -E "page|warn"`
Expected: 빌드는 통과하되 아직 `/en/` 라우트 파일이 없으므로 영어 페이지는 안 생긴다. 경고 없음.

이 시점에는 `en/` 글이 컬렉션에 로드되지만 라우트가 없어 렌더되지 않는다 — 정상이다. Task 4~5에서 라우트를 만든다.

- [ ] **Step 3: 커밋**

```bash
git add src/content/posts/en
git commit -m "영어 시드 글 추가 (TCP 에러 복구 번역본)"
```

---

### Task 4: 글 상세 본문 컴포넌트 추출과 영어 라우트

**Files:**
- Create: `src/components/PostArticle.astro`
- Modify: `src/pages/posts/[...slug].astro`
- Create: `src/pages/en/posts/[...slug].astro`

**Interfaces:**
- Consumes: `loadPosts`, `toSummary` from `src/lib/collection`; `relatedPosts`, `adjacentPosts`, `parseEntryId` from `src/lib/posts`
- Produces:
  - 컴포넌트 `PostArticle` — `{ entry: CollectionEntry<'posts'>; lang: string }`를 받아 메타·본문·목차·관련글·이전다음·읽기보조를 렌더
  - 라우트 `/en/posts/<slug>`

한국어 상세 페이지의 본문 로직을 컴포넌트로 빼내, 한국어·영어 라우트가 같은 컴포넌트를 쓴다. 라우트 파일에는 언어별로 다른 `getStaticPaths`만 남는다. "이 글이 무슨 언어인가"는 `parseEntryId` 하나로 판단한다.

- [ ] **Step 1: PostArticle 컴포넌트 작성**

`src/components/PostArticle.astro`:

```astro
---
import { render, type CollectionEntry } from 'astro:content';
import PostMeta from './PostMeta.astro';
import TableOfContents from './TableOfContents.astro';
import RelatedPosts from './RelatedPosts.astro';
import AdjacentNav from './AdjacentNav.astro';
import ReadingProgress from './ReadingProgress.astro';
import CodeCopyButton from './CodeCopyButton.astro';
import { loadPosts, toSummary } from '../lib/collection';
import { adjacentPosts, relatedPosts } from '../lib/posts';

interface Props {
  entry: CollectionEntry<'posts'>;
  lang: string;
}
const { entry, lang } = Astro.props;

const { Content, headings } = await render(entry);
const post = toSummary(entry);
const pool = await loadPosts(lang);

const related = relatedPosts(post, pool);
const { prev, next } = adjacentPosts(post, pool);
---

<ReadingProgress />

<div class="grid items-start gap-16 lg:grid-cols-[1fr_220px]">
  <article class="min-w-0 pt-14 pb-10">
    <PostMeta post={post} />
    <div class="prose">
      <Content />
    </div>
  </article>
  <TableOfContents headings={headings} />
</div>

<RelatedPosts posts={related} />
<AdjacentNav prev={prev} next={next} />
<CodeCopyButton />
```

- [ ] **Step 2: 한국어 라우트를 컴포넌트를 쓰도록 축소**

`src/pages/posts/[...slug].astro`를 아래로 교체한다.

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostArticle from '../../components/PostArticle.astro';
import { toSummary } from '../../lib/collection';
import { parseEntryId } from '../../lib/posts';

export async function getStaticPaths() {
  const entries = await getCollection('posts', ({ data }) => !data.draft);
  return entries
    .filter((e) => parseEntryId(e.id).lang === 'ko')
    .map((entry) => ({ params: { slug: parseEntryId(entry.id).slug }, props: { entry } }));
}

const { entry } = Astro.props;
const post = toSummary(entry);
---

<BaseLayout title={`${post.title} · 씨유`} description={post.description} active="archive">
  <PostArticle entry={entry} lang="ko" />
</BaseLayout>
```

- [ ] **Step 3: 영어 라우트 작성**

`src/pages/en/posts/[...slug].astro`:

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import PostArticle from '../../../components/PostArticle.astro';
import { toSummary } from '../../../lib/collection';
import { parseEntryId } from '../../../lib/posts';

export async function getStaticPaths() {
  const entries = await getCollection('posts', ({ data }) => !data.draft);
  return entries
    .filter((e) => parseEntryId(e.id).lang === 'en')
    .map((entry) => ({ params: { slug: parseEntryId(entry.id).slug }, props: { entry } }));
}

const { entry } = Astro.props;
const post = toSummary(entry);
---

<BaseLayout title={`${post.title} · 씨유`} description={post.description} active="archive">
  <PostArticle entry={entry} lang="en" />
</BaseLayout>
```

- [ ] **Step 4: 빌드하고 양쪽 라우트가 생성되는지 확인**

Run: `pnpm build 2>&1 | grep -E "posts/"`
Expected: `/posts/tcp-error-recovery/`(ko)와 `/en/posts/tcp-error-recovery/`(en) 둘 다 생성된다.

Run: `grep -o 'A Look at TCP Error Recovery' dist/en/posts/tcp-error-recovery/index.html | head -1`
Expected: 영어 제목이 나온다.

Run: `grep -o 'lang="en"' dist/en/posts/tcp-error-recovery/index.html | head -1`
Expected: `lang="en"` — Pagefind(3c)와 접근성이 이 속성에 의존한다.

- [ ] **Step 5: 관련글이 언어를 섞지 않는지 확인**

영어 글은 1편뿐이라 `relatedPosts`가 같은 언어 풀(`loadPosts('en')`)에서만 뽑으므로 관련글·이전다음이 비어야 한다. 한국어 글로 새지 않아야 한다.

Run: `grep -c '함께 읽으면 좋은 글' dist/en/posts/tcp-error-recovery/index.html`
Expected: `0` — 영어 관련글이 없으므로 섹션 자체가 렌더되지 않는다.

- [ ] **Step 6: 타입체크와 커밋**

Run: `pnpm check`
Expected: `0 errors`

```bash
git add -A
git commit -m "글 상세 본문을 PostArticle로 추출하고 영어 라우트 추가

한국어·영어 상세가 같은 컴포넌트를 쓰고, 라우트 파일에는 언어별
getStaticPaths만 남는다. 언어 판단은 parseEntryId 하나로 한다."
```

---

### Task 5: 아카이브 본문 컴포넌트 추출과 영어 아카이브

**Files:**
- Create: `src/components/ArchiveView.astro`
- Modify: `src/pages/posts/index.astro`
- Create: `src/pages/en/posts/index.astro`

**Interfaces:**
- Consumes: `loadPosts` from `src/lib/collection`; `CategoryFilter`
- Produces:
  - 컴포넌트 `ArchiveView` — `{ lang: string }`을 받아 아카이브 인트로 + 필터 목록을 렌더
  - 라우트 `/en/posts`

- [ ] **Step 1: ArchiveView 컴포넌트 작성**

`src/components/ArchiveView.astro`:

```astro
---
import CategoryFilter, { type FilterablePost } from './CategoryFilter';
import { loadPosts } from '../lib/collection';
import { formatShortDate, getYear } from '../lib/date';

interface Props {
  lang: string;
}
const { lang } = Astro.props;

const posts = await loadPosts(lang);

const items: FilterablePost[] = posts.map((p) => ({
  title: p.title,
  category: p.category,
  href: p.href,
  year: getYear(p.pubDate),
  meta: `${formatShortDate(p.pubDate)} · ${p.minutes}분`,
}));

const firstYear = posts.length
  ? getYear(posts[posts.length - 1].pubDate)
  : String(new Date().getUTCFullYear());
---

<section class="pt-16 pb-10">
  <div class="mb-4.5 text-xs font-semibold tracking-[0.16em] text-ink-faint uppercase">
    아카이브
  </div>
  <h1 class="m-0 text-[30px] leading-[1.15] font-extrabold tracking-[-0.03em] sm:text-[44px] sm:leading-[1.12]">
    기록의 목차
  </h1>
  <p class="mt-4 text-[17px] leading-relaxed text-ink-muted">
    {firstYear}년부터 남긴 {posts.length}편의 글. 카테고리로 골라 보거나, 아래에서
    연도별로 훑어보세요.
  </p>
</section>

<CategoryFilter posts={items} client:load />
```

- [ ] **Step 2: 한국어 아카이브를 컴포넌트를 쓰도록 축소**

`src/pages/posts/index.astro`를 아래로 교체한다.

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArchiveView from '../../components/ArchiveView.astro';
---

<BaseLayout title="기록의 목차 · 씨유" description="이동규가 남긴 글 목록." active="archive" narrow>
  <ArchiveView lang="ko" />
</BaseLayout>
```

- [ ] **Step 3: 영어 아카이브 작성**

`src/pages/en/posts/index.astro`:

```astro
---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ArchiveView from '../../../components/ArchiveView.astro';
---

<BaseLayout title="기록의 목차 · 씨유" description="이동규가 남긴 글 목록." active="archive" narrow>
  <ArchiveView lang="en" />
</BaseLayout>
```

- [ ] **Step 4: 빌드하고 양쪽 아카이브 확인**

Run: `pnpm build 2>&1 | grep -E "posts/index"`
Expected: `/posts/index.html`과 `/en/posts/index.html` 둘 다 생성된다.

Run: `grep -o 'A Look at TCP Error Recovery' dist/en/posts/index.html | head -1`
Expected: 영어 아카이브에 영어 글이 나온다.

Run: `grep -c 'TCP 에러 복구' dist/en/posts/index.html`
Expected: `0` — 영어 아카이브에 한국어 글이 섞이지 않는다.

- [ ] **Step 5: 타입체크와 커밋**

Run: `pnpm check`
Expected: `0 errors`

```bash
git add -A
git commit -m "아카이브 본문을 ArchiveView로 추출하고 영어 아카이브 추가"
```

---

### Task 6: 언어 전환 컨트롤

**Files:**
- Create: `src/lib/i18n.ts`
- Create: `src/lib/i18n.test.ts`
- Create: `src/components/LangSwitch.astro`
- Modify: `src/layouts/BaseLayout.astro`
- Modify: `src/components/Header.astro`
- Modify: `src/pages/posts/[...slug].astro`
- Modify: `src/pages/en/posts/[...slug].astro`
- Modify: `src/pages/posts/index.astro`
- Modify: `src/pages/en/posts/index.astro`

**Interfaces:**
- Consumes: `PostSummary`, `parseEntryId` from `src/lib/posts`
- Produces:
  - `interface AltLocale { current: 'ko' | 'en'; href: string }`
    — `current`는 **지금 페이지의 언어**, `href`는 **반대 언어**로 갈 URL. 반전 계산을
    한 군데도 두지 않기 위해 이렇게 정의한다.
  - `translationHref(slug: string, targetLang: 'ko' | 'en', entries: { id: string }[]): string`
    — 번역이 있으면 그 글의 URL, 없으면 그 언어의 아카이브 URL(404를 내지 않게 한다)

전환 컨트롤은 크롬이 아니라 "번역이 의미 있는 곳"(글 상세·아카이브)에만 둔다. `BaseLayout`이 `altLocale` prop을 받고, 있으면 헤더가 KO/EN 컨트롤을 렌더한다. 홈·기술·태그 페이지는 이 prop을 넘기지 않으므로 컨트롤이 안 나온다 — 아직 영어판이 없는 페이지에서 가짜 링크를 만들지 않기 위해서다.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/i18n.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { translationHref } from './i18n';

const entries = [
  { id: 'ko/tcp-error-recovery' },
  { id: 'en/tcp-error-recovery' },
  { id: 'ko/load-balancer' },
];

describe('translationHref', () => {
  it('번역이 있으면 그 글의 URL을 낸다', () => {
    expect(translationHref('tcp-error-recovery', 'en', entries)).toBe('/en/posts/tcp-error-recovery');
    expect(translationHref('tcp-error-recovery', 'ko', entries)).toBe('/posts/tcp-error-recovery');
  });

  it('번역이 없으면 그 언어의 아카이브로 보낸다', () => {
    // load-balancer는 en 번역이 없다. 404 대신 영어 아카이브로.
    expect(translationHref('load-balancer', 'en', entries)).toBe('/en/posts');
  });

  it('한국어 아카이브 fallback은 prefix가 없다', () => {
    expect(translationHref('nonexistent', 'ko', entries)).toBe('/posts');
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `pnpm test src/lib/i18n.test.ts`
Expected: FAIL — `Failed to resolve import "./i18n"`

- [ ] **Step 3: 구현**

`src/lib/i18n.ts`:

```ts
import { parseEntryId } from './posts';

/**
 * 전환 대상 언어에 이 글의 번역이 있으면 그 글의 URL을, 없으면 그 언어의
 * 아카이브 URL을 낸다. 번역이 없다고 없는 페이지로 링크해 404를 내지 않기 위해서다.
 *
 * 한국어는 prefix가 없고(prefixDefaultLocale: false) 영어는 /en/ 을 붙인다.
 */
export function translationHref(
  slug: string,
  targetLang: 'ko' | 'en',
  entries: { id: string }[],
): string {
  const prefix = targetLang === 'ko' ? '' : '/en';
  const hasTranslation = entries.some((e) => {
    const parsed = parseEntryId(e.id);
    return parsed.lang === targetLang && parsed.slug === slug;
  });
  return hasTranslation ? `${prefix}/posts/${slug}` : `${prefix}/posts`;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm test src/lib/i18n.test.ts`
Expected: PASS — 3 passed

- [ ] **Step 5: LangSwitch 컴포넌트**

`src/components/LangSwitch.astro`:

```astro
---
interface Props {
  current: 'ko' | 'en';
  /** 반대 언어로 갈 URL */
  altHref: string;
}
const { current, altHref } = Astro.props;
const alt = current === 'ko' ? 'en' : 'ko';
const label = { ko: '한국어', en: 'English' };
---

<div class="flex items-center gap-1 text-[12.5px] font-medium">
  <span class="text-ink" aria-current="true">{label[current].slice(0, 2).toUpperCase()}</span>
  <span class="text-ink-faint" aria-hidden="true">/</span>
  <a href={altHref} class="text-ink-subtle transition-colors hover:text-ink">
    {label[alt].slice(0, 2).toUpperCase()}
  </a>
</div>
```

- [ ] **Step 6: BaseLayout이 altLocale를 받아 헤더에 넘긴다**

`src/layouts/BaseLayout.astro`의 Props에 추가한다.

```ts
import type { AltLocale } from '../lib/i18n';
```

```ts
  /** 언어 전환 컨트롤을 띄울 때. 없으면 컨트롤이 안 나온다. */
  altLocale?: AltLocale;
```

구조분해에 `altLocale`을 더하고, `<Header active={active} />`를 `<Header active={active} altLocale={altLocale} />`로 바꾼다.

그리고 `src/lib/i18n.ts`에 타입을 추가한다.

```ts
export interface AltLocale {
  /** 지금 페이지의 언어. 반대 언어로 갈 href와 짝이다. */
  current: 'ko' | 'en';
  href: string;
}
```

- [ ] **Step 7: Header가 컨트롤을 렌더한다**

`src/components/Header.astro`의 Props에 `altLocale?: AltLocale`를 더하고 import한다. 데스크톱 nav의 `글 검색` 알약 뒤(즉 `</nav>` 앞)에 조건부로 넣는다.

```astro
      {altLocale && <LangSwitch current={altLocale.current} altHref={altLocale.href} />}
```

`altLocale.current`가 곧 지금 페이지의 언어이므로 반전 계산이 없다. import도 추가한다:

```ts
import LangSwitch from './LangSwitch.astro';
import type { AltLocale } from '../lib/i18n';
```

모바일 패널에도 같은 조건부 블록을 `글 검색` 링크 뒤에 넣는다.

- [ ] **Step 8: 네 라우트가 altLocale을 계산해 넘긴다**

각 라우트에서 `getCollection`으로 엔트리 목록을 얻어 `translationHref`에 넘긴다.

`src/pages/posts/[...slug].astro`의 프런트매터 끝에 추가하고 BaseLayout에 prop을 넘긴다:

```ts
import { getCollection } from 'astro:content';
import { translationHref } from '../../lib/i18n';
```

```ts
const allEntries = await getCollection('posts');
const slug = parseEntryId(entry.id).slug;
// current는 이 페이지의 언어(ko), href는 반대 언어(en)로 갈 곳.
const altLocale = { current: 'ko' as const, href: translationHref(slug, 'en', allEntries) };
```

`<BaseLayout ... altLocale={altLocale}>`. `getCollection`을 getStaticPaths에서 쓰고 있어도 그건 격리 스코프라, 프런트매터에서 `allEntries`를 따로 불러야 한다.

`src/pages/en/posts/[...slug].astro`는 대칭으로 `current: 'en'`, `translationHref(slug, 'ko', allEntries)`.

`src/pages/posts/index.astro`(ko 아카이브)는 slug가 없으므로 아카이브끼리 잇는다:

```ts
const altLocale = { current: 'ko' as const, href: '/en/posts' };
```

`src/pages/en/posts/index.astro`는 `{ current: 'en' as const, href: '/posts' }`.

- [ ] **Step 9: 왕복 전환을 브라우저로 확인**

`pnpm build && pnpm preview` 후:

- `/posts/tcp-error-recovery`(번역 있음) 헤더의 `EN`을 클릭 → `/en/posts/tcp-error-recovery`로 이동, 영어 본문이 뜬다. 거기서 `KO`를 클릭 → 한국어 글로 돌아온다.
- `/posts/load-balancer`(번역 없음)의 `EN`을 클릭 → `/en/posts`(영어 아카이브)로 간다. **404가 아니다.**
- `/`(홈)과 `/tags`에는 언어 컨트롤이 없다.

computed style이 아니라 실제 이동 결과(URL·본문)로 판정한다.

- [ ] **Step 10: 타입체크와 커밋**

Run: `pnpm test && pnpm check && pnpm build`
Expected: 전부 통과

```bash
git add -A
git commit -m "언어 전환 컨트롤 추가

번역이 있으면 그 글로, 없으면 그 언어의 아카이브로 보내 404를 막는다.
컨트롤은 번역이 의미 있는 글·아카이브에만 뜨고, 아직 영어판이 없는
홈·기술·태그에는 나오지 않는다."
```

---

### Task 7: sitemap · RSS의 언어 처리 점검

**Files:**
- Modify: `src/pages/rss.xml.ts` (필요 시)
- 확인: `astro.config.mjs`의 sitemap

**Interfaces:**
- Consumes: `loadPosts` from `src/lib/collection`
- Produces: 없음 (기존 출력 점검·보정)

RSS는 지금 한국어만 낸다(`loadPosts('ko')`). 이건 의도된 동작이므로 그대로 둔다 — 한국어 독자용 피드다. 다만 영어 라우트가 sitemap에 정상 포함되는지, canonical이 언어별로 맞는지 확인한다.

- [ ] **Step 1: sitemap에 영어 라우트가 들어갔는지 확인**

Run: `pnpm build && grep -o '/en/posts/tcp-error-recovery' dist/sitemap-*.xml | head -1`
Expected: 영어 글 URL이 sitemap에 있다.

- [ ] **Step 2: 언어별 canonical 확인**

Run: `grep -o '<link rel="canonical"[^>]*>' dist/en/posts/tcp-error-recovery/index.html`
Expected: canonical이 `https://brainbackdoor.github.io/en/posts/tcp-error-recovery`를 가리킨다(한국어 URL이 아니다).

미달이면 `BaseLayout`의 canonical 계산(`new URL(Astro.url.pathname, Astro.site)`)이 `Astro.url.pathname`을 쓰는지 확인한다 — 이 값은 라우트별로 다르므로 정상이어야 한다. 문제가 있으면 여기서 보정한다.

- [ ] **Step 3: RSS가 한국어만 내는지 확인**

Run: `grep -c '<item>' dist/rss.xml`
Expected: `3` — 한국어 글 3편만. 영어 글은 포함되지 않는다(의도).

- [ ] **Step 4: 커밋 (보정이 있었을 때만)**

보정이 없었으면 이 태스크는 확인만으로 끝난다. 있었으면:

```bash
git add -A
git commit -m "영어 라우트의 canonical·sitemap 처리 점검"
```

---

## 3b단계 완료 기준

- `/tags`에서 태그를 눌러 `/tags/<tag>`로 이동하고, 글 상세의 태그도 링크로 동작한다(한글 태그 포함)
- `/en/posts`와 `/en/posts/<slug>`가 생성되고 영어 본문이 뜨며, 한국어 글이 섞이지 않는다
- 번역이 있는 글에서 KO↔EN 전환이 왕복하고, 번역이 없는 글의 전환은 아카이브로 떨어져 404가 안 난다
- 홈·기술·태그에는 언어 컨트롤이 없다
- 영어 페이지의 `<html lang="en">`과 canonical이 맞다(3c Pagefind가 이 lang에 의존)
- `pnpm test`·`pnpm check`·`pnpm build` 통과

## 3c로 넘기는 것

- **Pagefind 검색** — postbuild 인덱싱, 검색 페이지·UI, 헤더 `글 검색` 알약 연결. `<html lang>`이 이미 언어별로 맞아 다국어 인덱싱은 자동으로 분리된다.
- 영어 소개 페이지, UI 문구 번역(원할 때)
- giscus 댓글(저장소 생성 후)
